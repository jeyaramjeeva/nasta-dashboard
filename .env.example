# Free Supabase project → Settings → API
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key_here

# Shared password for the Upload Excel page (+ local login gate if no Supabase)
VITE_UPLOAD_PASSWORD="Nasta998#"

# Guest login (stall view only).
VITE_GUEST_PASSWORD="Guest9987"

# PayPal.me link shown as QR on delivery
VITE_PAYPAL_ME_URL="https://paypal.me/nastazentrum"

# Team allowlist — only these emails can sign in (email:DisplayName)
# Create matching users in Supabase → Authentication → Users
# Always include guest@nastazentrum.de (Guest) — or the app adds Guest automatically
VITE_ALLOWED_EMAILS="jeeva@yourmail.com:Jeeva,sriram@yourmail.com:Sriram,sneha@yourmail.com:Sneha,developer@nastazentrum.de:Developer,guest@nastazentrum.de:Guest"

# Forgot-password requests are emailed to jeevajeyaraam@gmail.com (FormSubmit).
# First request may need one confirmation click in that inbox.
# Also add https://nastazentrum.vercel.app/account to Supabase Auth redirect URLs.

# —— Capacitor mobile (see MOBILE.md) ——
# CAP_SERVER_URL=https://nastazentrum.vercel.app
# CAP_BUNDLE=1

# —— Server-only (Vercel → Environment Variables; NOT VITE_) ——
# Required for Excel Publish / upload history (bypasses Supabase RLS)
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here
# Optional alias for upload password on the server:
# UPLOAD_PASSWORD="Nasta998#"
#
# Jeeva AI Code Agent (Cursor Cloud Agents API)
# CURSOR_API_KEY=
# CURSOR_REPO_URL=https://github.com/jeyaramjeeva/nasta-dashboard
# CURSOR_REPO_REF=main
# CURSOR_MODEL_ID=composer-2.5
#
# Optional smarter AI helper for Sriram/Sneha (falls back to built-in guide)
# OPENAI_API_KEY=
# AI_HELPER_MODEL=gpt-4o-mini
